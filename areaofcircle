#include<stdio.h>
#include<math.h>
void areaOfCircle(double r){
    double area = 0;
    area = 3.14*pow(r,2);
    printf("Area of circle : %0.4f\n", area);

}
int main(){
    double r;    
    printf("Enter radius of circle: ");
    scanf("%lf", &r);
    areaOfCircle(r);
    return 0;
}
